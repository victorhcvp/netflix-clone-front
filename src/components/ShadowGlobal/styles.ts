import styled from 'styled-components';

export const ShadowGlobalDiv = styled.div`
  position: absolute;
  top: -20px;
  left: 0;
  width: 100%;
  height: 20px;
  box-shadow: 0px 29px 36px 4px rgb(0 0 0);
  z-index: 0;
`;
